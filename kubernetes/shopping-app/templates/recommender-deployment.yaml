apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommender
spec:
  replicas: 1
  strategy: {}
  selector:
    matchLabels:
      service: recommender
  template:
    metadata:
      labels:
        service: recommender
    spec:
      containers:
        - image: recommender
          name: recommender
          ports:
            - containerPort: 50051
              protocol: TCP
            - containerPort: 3001
              protocol: TCP
          resources: {}
          imagePullPolicy: IfNotPresent
      restartPolicy: Always
status: {}
